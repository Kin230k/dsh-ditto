# DSH Ditto

The user wants a new native DSH plugin for ordinary users: sample-first batch document work, provisional name DSH Ditto / 照樣做. Do not inherit KeepGate governance or claim stronger models. All implementation code is assigned to GPT-5.6 Terra by explicit user instruction; coordinator owns planning and review.

Current milestone is M0: a working, clearly labelled prototype of preview/edit/copy/save-recipe, not the full AI-learning product. Product thesis: first show representative real outputs, let the user correct them, apply the reviewed plan to a batch, reuse a recipe for later batches. First operation only creates renamed/classified copies in a new destination; preserve original files. Semantic AI recipe inference and arbitrary document transformations are later milestones unless actually implemented and tested.

Work only in this project. Keep C:/Users/user/Desktop/KeepGate and C:/Users/user/Desktop/deepseek-harness unchanged. Never read .env or credentials. DSH target is published 0.1.5-rc.1, independently verify package APIs; latest GitHub release 0.1.5-rc.2 is the canary. Isolate DSH_HOME and use keyless tests. Do not globally update DSH, install into user's live profile, publish or push.

Implementation principles: small TypeScript ESM packages; versioned declarative recipes with no eval, scripts or shell command payloads; bounded inputs; validate persisted/model/user boundaries. Preview is read-only. Apply must use the exact reviewed plan identity and reject stale source hashes, changed plan revision, collisions, traversal, symlink escapes and destination overwrites. Keep input extension. Per-item statuses and durable resume must reflect actual disk effects. Do not rerun completed outputs or silently overwrite anything. Store only required local plan/recipe metadata. Native DSH tools must use current published public APIs, no core patches or claimed compatibility from mocks alone.

UI is Traditional Chinese, approachable, clean, roomy and useful on a small laptop. Show real file before/after names, 3 representative samples, editable destination names, a clear primary action and per-item outcomes. Avoid coding jargon, fake AI claims, decorative dashboards and pretend buttons. Demo data and simulated actions must be labelled. Use ui-ux-pro-max skill for UI work. No network fonts/trackers. No new standalone website deployment.

Meaningful verification: preview has zero writes to sources/destination; original hashes unchanged after success/failure; duplicate/case-insensitive collision rejected; source edit between preview/apply rejected; path escapes/symlinks denied; duplicate apply and restart do not duplicate output; actual output bytes match originals; review digest changes on edits. Check real native DSH tool registration/execution with isolated host when available. Root reviews results independently.
