# Second discussion: general consumers, not coding agents

The user clarified: target ordinary nontechnical people and a breakout consumer product; we may abandon KeepGate entirely. Do not remain anchored to coding verification. Reply as a product co-designer, analysis only, no tools/code, Traditional Chinese, under 900 words.

Corrections since first brief: latest KeepGate untracked single trial on 2026-09-04 actually solved django-13344 (F2P 2/2), still observe with evidence-quality flags off. Earlier results were observe, not strict. Correct conclusion is no demonstrated causal accuracy improvement, not that all current versions fail.

Primary-source competitor findings: bpc-oss/dsh-verification already has content-addressed evidence and old-PASS defenses; snow-The/dsh-session-handoff handles handoff; Shizuku-keop/dsh-compat-guard covers upgrade preflight/backup/boot registry; 23swccp/dsh-undo and many others do rollback; Culeot/dsh-batch-pipeline does persistent batch/resume; 9Epuuuu/dsh-plugin-folder-tidy organizes folders; uckkk/dsh-file-rename does batch rename/dry run. These eliminate generic versions of those ideas.

Consumer candidates:
A. DSH Compare Desk (leading): Drop several proposals, itineraries, course brochures or product sheets and say what matters. Get a persistent side-by-side comparison board. Every filled cell links to an exact quotation in a named source; missing information stays visibly missing; multiple claims/conflicts stay visible; source changes invalidate affected cells. Click a value to see its original words. Export a self-contained offline HTML comparison that a friend/colleague can open without DSH/account. No auto winner, purchase or fabricated completeness. Wedge: make 'which offer actually includes what' easy to inspect and share; durable editable comparison artifact, not another document chat. MVP native DSH tool + local preview/export, text/PDF/DOCX import where safely supported, 3-document demo. AI extraction uses user's existing DSH model; deterministic provenance checks validate quote existence only, not semantic correctness. Explicit human corrections distinct from sourced values. Avoid pretending same-model quotes prove interpretations.
B. 'One good sample, then the rest': show a real output on one file, user accepts format, apply to remaining files with per-item preview/retry; saved consumer recipe. More repeat use, but broad arbitrary transformation means expensive/frail execution and existing batch pipeline.
C. 'Share safely': user drops documents and screenshots; local configurable redaction preview with consistent pseudonyms, export sanitized copies and reversible local mapping. Broad pain but mature competitor category and false-negative privacy risk.

For A challenge NotebookLM, spreadsheets with AI and plain DSH prompt. Is this a feature or a compelling small plugin? Choose A/B/C or a clearly stronger concrete consumer idea. Give one-sentence promise, first 60-second demo, minimal first slice, distribution/retention hypothesis, 5 stop/go measurements (targets, not claimed evidence), and 3 technical risks. Do not claim worldwide uniqueness or validated mass demand. We want a useful scoped prototype and roadmap now, not months of generic infrastructure.
