# Final product challenge — corrected current competitor evidence

We read your previous responses. You recommended Compare Desk, but the claim that NotebookLM has no persistent tables is outdated: Google officially announced Data Tables on 2025-12-18, with multi-source structured tables and Sheets export incl. citations (https://blog.google/innovation-and-ai/models-and-research/google-labs/notebooklm-data-tables/ ; https://support.google.com/gemininotebook/answer/16206563). Your earlier competitor assertions must be treated as hypotheses, not verified facts.

After independent primary-source reviews we provisionally choose B, much narrower: DSH Ditto / 照樣做. For ordinary users with 10–50 messy documents: show 3 real sample filenames/classifications, let user correct results, derive a readable declarative recipe, preview the complete batch, create organized COPIES in a fresh output folder, show each success/exception, save recipe and reuse on next batch. No arbitrary executable transformations, no original modification/deletion, no unsupervised scheduled work. Existing DSH model reads/extracts data and proposes filenames; no second paid model. No guessing missing dates or source names.

Competitor sources verified: folder-tidy is fixed extension classification/no preview; file-rename does regex/prefix/case dry-run; batch-pipeline is ledger/resume/no consumer UI; action-outbox stages arbitrary tool calls rather than sample-based document teaching. Full sample correction→recipe→batch UX not located in reviewed DSH repos, not a worldwide novelty claim.

Terra is implementing M0 now: deterministic templates and reviewed per-file name overrides, actual copies, stale source/hash checks, non-overwrite/path constraints, persistent statuses/retry, local Chinese UI and current public DSH tools. M0 explicitly does not claim semantic learning. M1 adds model interpretation of sample corrections and multi-document preview. We prefer frequent daily work over infrequent personal comparison, but this is unvalidated.

Respond in Traditional Chinese under 600 words, analysis only (no tools/code). Do you accept this narrowed choice over your earlier A recommendation? What is the strongest reason to kill it? Give three concrete product improvements, a 5-person first test script, and what M0 absolutely must not be marketed as. Please critique honestly, don't agree just because implementation started.
